{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/input/file/transformation/spec/v1alpha1/schemas/FileInputSpec.schema.json",
  "title": "FileInputSpec",
  "type": "object",
  "description": "FileInputSpec is the input specification for the FileInput transformation.",
  "properties": {
    "compress": {
      "type": "boolean",
      "description": "Compress indicates whether the file should be compressed with gzip."
    },
    "mediaType": {
      "type": "string",
      "description": "MediaType is the optional media type of the file.\nIf not set, it is auto-detected."
    },
    "outputPath": {
      "type": "string",
      "description": "OutputPath is the optional directory path to buffer the blob file.\nIf empty, a temporary file will be created."
    },
    "path": {
      "type": "string",
      "description": "Path is the path to the file on the local filesystem."
    },
    "workingDirectory": {
      "type": "string",
      "description": "WorkingDirectory is the base directory for resolving relative paths."
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false
}