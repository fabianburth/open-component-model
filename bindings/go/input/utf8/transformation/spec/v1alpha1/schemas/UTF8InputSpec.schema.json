{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/input/utf8/transformation/spec/v1alpha1/schemas/UTF8InputSpec.schema.json",
  "title": "UTF8InputSpec",
  "type": "object",
  "description": "UTF8InputSpec is the input specification for the UTF8Input transformation.",
  "properties": {
    "compress": {
      "type": "boolean",
      "description": "Compress indicates whether the content should be compressed with gzip."
    },
    "formattedJson": {
      "type": "object",
      "description": "FormattedJSON is a JSON object, raw encoded via UTF-8, with default indentation applied.",
      "additionalProperties": true
    },
    "json": {
      "type": "object",
      "description": "JSON is a JSON object, raw encoded via UTF-8.",
      "additionalProperties": true
    },
    "outputPath": {
      "type": "string",
      "description": "OutputPath is the optional directory path to buffer the blob file.\nIf empty, a temporary file will be created."
    },
    "text": {
      "type": "string",
      "description": "Text is a UTF-8 string, raw encoded."
    },
    "yaml": {
      "type": "object",
      "description": "YAML is a YAML object, raw encoded via UTF-8.",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}